import { notFound } from "next/navigation"; import Link from "next/link"; import { resources } from "../../../content/resources";
export function generateStaticParams(){return resources.map(r=>({slug:r.slug}))}
export default async function ResourcePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const r=resources.find(x=>x.slug===slug);if(!r)notFound();return <article className="content shell resource-body"><header className="page-hero"><div className="eyebrow">{r.type}</div><h1>{r.title}</h1><p>{r.summary}</p></header><h2>The executive takeaway</h2><p>Owner independence grows when information, decisions, and critical work become visible and repeatable. The practical starting point is not a large transformation; it is identifying the dependency that creates the most recurring pressure.</p><h2>What to do next</h2><div className="callout"><strong>{r.action}</strong></div><p>Use the result to locate the missing information, ownership, or operating rule. Then improve the system before adding technology.</p><Link className="button" href="/diagnostic">Review the Owner Independence Diagnostic →</Link></article>}
