import Link from "next/link"; import { PageHero } from "../../components/SiteParts"; import { categories,resources } from "../../content/resources";
export const metadata={title:"Founder Resources",description:"Videos and practical guides for building a less owner-dependent business."};
export default function Resources(){return <><PageHero eyebrow="Founder Resources" title="Practical thinking for a business that can run without you.">Curated by the executive problem you are trying to solve—not by publication date or content format.</PageHero><div className="shell callout"><div className="eyebrow">Free scorecard</div><h2>Can your business run without you?</h2><p>Use 15 questions to identify where information, decisions, relationships, and critical work still depend on the owner.</p><a className="button secondary" href="/owner-independence-scorecard.pdf" download>Download the Scorecard →</a></div><div className="content shell">{categories.map(c=><section className="category" key={c.id}><div className="section-head"><div><div className="eyebrow">Resource pathway</div><h2>{c.title}</h2></div><p>{c.description}</p></div><div className="resource-grid">{resources.filter(r=>r.category===c.id).map(r=><Link className="resource-card" href={`/founder-resources/${r.slug}`} key={r.slug}><div className="meta">{r.type}</div><h3>{r.title}</h3><p>{r.summary}</p><strong>Explore →</strong></Link>)}</div></section>)}</div></>}
